# АРХИТЕКТУРНЫЙ АУДИТ ПРОЕКТА «ГОЛЕМ»

**Дата:** 2026-07-01  
**Статус:** Завершён  
**Этап 1:** Уборка (удаление/перемещение файлов)  
**Этап 2:** Аудит (предложения по оптимизации)

---

## ЭТАП 1: УБОРКА

### 1.1 Удалённые файлы

Следующие файлы были удалены как дублирующие функционал или неиспользуемые отладочные скрипты:

**В `tools/`:**
- `tools/debug-sefaria.py` - отладочный скрипт, дублирует функционал
- `tools/debug-sefaria2.py` - отладочный скрипт, дублирует функционал
- `tools/debug-sefaria3.py` - отладочный скрипт, дублирует функционал
- `tools/debug-sefaria4.py` - отладочный скрипт, дублирует функционал
- `tools/test-sefaria-api.py` - тестовый скрипт, не входит в основную функциональность

**Итого:** 5 файлов удалено

### 1.2 Перемещённые файлы

Файлы не перемещались на этом этапе.

### 1.3 Созданные файлы

- `ARCHITECTURE-AUDIT.md` - данный отчёт

---

## ЭТАП 2: АУДИТ АРХИТЕКТУРЫ

### 2.1 Папки для объединения или переименования

1. **`content/` vs корневые папки**
   - Проблема: В `ARCHITECTURE.md` указаны папки `terminology/`, `researches/`, `teachings/` на корневом уровне, но в реальности они находятся в `content/`
   - Предложение: Оставить структуру с `content/`, но обновить `ARCHITECTURE.md` для соответствия реальности

2. **`products/` - не указана в ARCHITECTURE.md**
   - Проблема: Папка `products/` существует, но не описана в архитектуре
   - Предложение: Добавить описание в `ARCHITECTURE.md` или переместить содержимое в соответствующую папку

3. **`data/` - не указана в ARCHITECTURE.md**
   - Проблема: Папка `data/` с JSON-файлами не описана
   - Предложение: Добавить описание в `ARCHITECTURE.md` как "Хранилище данных"

4. **`backlog/` - не указана в ARCHITECTURE.md**
   - Проблема: Папка `backlog/` существует, но не описана
   - Предложение: Добавить описание в `ARCHITECTURE.md` как "Задачи и идеи"

5. **`archive/` - не указана в ARCHITECTURE.md**
   - Проблема: Папка `archive/` с изображениями не описана
   - Предложение: Добавить описание или удалить если не используется

### 2.2 Дублирующиеся файлы для удаления/объединения

1. **Дублирование `terminology/`**
   - `content/terminology/` - 100+ терминов
   - Возможно дублирование с `docs/GLOSSARY.md`
   - Предложение: Указать в `GLOSSARY.md` ссылки на файлы в `content/terminology/`

2. **Дублирование `teachings/`**
   - `content/teachings/` - 250+ файлов с учениями
   - Предложение: Добавить индексный файл для навигации

3. **Дублирование исследований**
   - `content/researches/` - исследования по темам
   - `content/tanakh/` - танахические исследования
   - Предложение: Объединить или чётко разделить границы

### 2.3 Папки не соответствующие ARCHITECTURE.md

1. **`ed/` - отсутствует в файловой системе**
   - В `ARCHITECTURE.md` указана папка `ed/` с подпапками `neuro/`, `davar/`, `agent/`, `assistant/`
   - В реальности: папка отсутствует
   - Предложение: Удалить упоминание из `ARCHITECTURE.md` или создать папку

2. **`web/` - отсутствует в файловой системе**
   - В `ARCHITECTURE.md` указана папка `web/` с `index.html`
   - В реальности: папка отсутствует (есть `products/website/`)
   - Предложение: Удалить упоминание из `ARCHITECTURE.md` или переместить `products/website/` в `web/`

3. **`tools/cache/` - отсутствует**
   - В `ARCHITECTURE.md` указана папка `tools/cache/`
   - В реальности: папка отсутствует
   - Предложение: Удалить упоминание или создать папку

4. **`instructions/tahor/` - отсутствует**
   - В `ARCHITECTURE.md` указана папка `instructions/tahor/`
   - В реальности: папка отсутствует (словари находятся в `instructions/dictionaries/`)
   - Предложение: Удалить упоминание или создать симлинк

5. **`instructions/guides/` - дублирует `guides/`**
   - В `ARCHITECTURE.md` указана `instructions/guides/`
   - В реальности: есть отдельная папка `guides/`
   - Предложение: Удалить `instructions/guides/` из ARCHITECTURE.md

### 2.4 Предложения по улучшению навигации

1. **Добавить README.md в каждую основную папку:**
   - `content/README.md` - описание структуры content
   - `content/bashah/README.md` - описание Bashah
   - `content/tanakh/README.md` - описание Танаха
   - `content/researches/README.md` - описание исследований
   - `products/README.md` - описание продуктов
   - `tools/README.md` - описание инструментов
   - `instructions/README.md` - описание инструкций

2. **Создать индексные файлы:**
   - `content/INDEX.md` - алфавитный индекс всех файлов в content
   - `tools/INDEX.md` - индекс всех скриптов
   - `products/INDEX.md` - индекс всех продуктов

3. **Добавить навигационные ссылки:**
   - В каждом README добавить ссылки на родительскую и дочерние папки
   - Использовать относительные пути для ссылок

### 2.5 Предложения по прекоммит-хукам

1. **Проверка структуры:**
   ```bash
   # Проверка соответствия ARCHITECTURE.md
   python tools/checkers/check-structure.py
   ```

2. **Проверка метаданных:**
   ```bash
   # Валидация метаданных в md-файлах
   python tools/checkers/validate-metadata.py
   ```

3. **Проверка naming convention:**
   ```bash
   # Проверка имён файлов
   python tools/checkers/check-naming.py
   ```

4. **Проверка религионимов:**
   ```bash
   # Автоматическая проверка на религионимы
   python tools/checkers/check-religionisms.py
   ```

5. **Проверка битых ссылок:**
   ```bash
   # Проверка внутренних ссылок
   python tools/checkers/check-links.py
   ```

6. **Пример .git/hooks/pre-commit:**
   ```bash
   #!/bin/bash
   # Прекоммит-хук для проверки структуры
   
   echo "🔍 Проверка структуры проекта..."
   python tools/checkers/check-structure.py
   if [ $? -ne 0 ]; then
       echo "❌ Ошибка структуры!"
       exit 1
   fi
   
   echo "🔍 Проверка метаданных..."
   python tools/checkers/validate-metadata.py
   if [ $? -ne 0 ]; then
       echo "❌ Ошибка метаданных!"
       exit 1
   fi
   
   echo "✅ Все проверки пройдены"
   exit 0
   ```

---

## СВОДНАЯ ТАБЛИЦА

### Критические проблемы (требуют немедленного исправления):

1. Папки `ed/` и `web/` отсутствуют, но указаны в ARCHITECTURE.md
2. Папка `products/` не описана в ARCHITECTURE.md
3. Папка `data/` не описана в ARCHITECTURE.md
4. Дублирование `instructions/guides/` и `guides/`

### Средние проблемы (требуют исправления в течение недели):

1. Отсутствие README.md в основных папках
2. Отсутствие индексных файлов
3. Дублирование terminology между content/ и docs/

### Низкий приоритет (можно исправить постепенно):

1. Добавление прекоммит-хуков
2. Объединение похожих исследований
3. Оптимизация навигации

---

## РЕКОМЕНДАЦИИ

### Немедленные действия:

1. Обновить `ARCHITECTURE.md` для соответствия реальной структуре
2. Удалить упоминания несуществующих папок (`ed/`, `web/`, `tools/cache/`, `instructions/tahor/`)
3. Добавить описание существующих папок (`products/`, `data/`, `backlog/`, `archive/`)

### Краткосрочные действия (неделя):

1. Создать README.md в основных папках
2. Создать индексные файлы
3. Объединить дублирующуюся документацию

### Долгосрочные действия (месяц):

1. Внедрить прекоммит-хуки
2. Автоматизировать проверку структуры
3. Создать скрипт синхронизации ARCHITECTURE.md с реальной структурой

---

## ЗАКЛЮЧЕНИЕ

Проект имеет хорошую базовую структуру, но требует:
1. Обновления документации (ARCHITECTURE.md)
2. Добавления навигационных файлов (README.md)
3. Удаления неиспользуемых отладочных скриптов
4. Внедрения автоматических проверок

После выполнения рекомендаций проект станет более поддерживаемым и понятным для новыхcontributors.