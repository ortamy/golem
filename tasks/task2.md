ЗАДАЧА: Клуб MVP — страница с карточками исследований, стек аватарок,
деталь с комментариями и вложениями

КОНТЕКСТ
- Research Lab, Vanilla JS; маршрут #club сейчас = заглушка с waitlist.
- Роли: js/roles.js (guest/researcher/admin); dev-селектор ролей есть.
- Supabase ещё не подключён → данные через адаптер: Supabase если есть,
  иначе MOCK (js/club-data.js) для теста UI. build/ — производный; собрать.

=== 1. СТРАНИЦА #club (заменяет заглушку) ===
1.1. Шапка: «ГОЛЕМ · КЛУБ · СВИВА ИССЛЕДОВАТЕЛЕЙ» + бейдж [скоро] (флаг,
     снимется при релизе). Подзаголовок про живую кеhилу.
     Ограничения общей шапки: не скрывать `.lab-hero` для `club`; порядок
     элементов — иконка + H1, отдельная плашка, затем подзаголовок. Плашка
     не должна быть вложена в H1 или слипаться с его текстом.
1.2. Баннер СЛОВО ДНЯ: первое слово из words.json тренажёра
     (глифы + глосса) + [Собрать →] (→ тренажёр) + [Обсудить] (→ деталь).
1.3. Раскладка: слева лента карточек, справа сайд-панель
     (Сейчас в клубе / Ближайшая сессия / Мои круги / Мой профиль — стабы;
     для гостя в панели — waitlist из старой заглушки).
1.4. Mobile: одна колонка, панель под лентой.

=== 2. КАРТОЧКА ИССЛЕДОВАНИЯ ===
2.1. Поля: тип-бейдж (стих/слово/состояние/доска), ref, title, synthesis-сниппет.
2.2. СТЕК АВАТАРОК: перекрывающиеся кружки (margin-left:-8px), первые 5,
     дальше кружок «+N»; рядом «· M постов». Клик по аватарке → мини-профиль
     (handle, уровень 𐤀–, плоды).
2.3. Счётчики: эмет: X · вопрос: Y.
2.4. Кнопки: [Открыть ↗] (deep-link по attachment.route) и [Вступить].
2.5. Клик по карточке → деталь.

=== 3. ДЕТАЛЬ #club/<id> ===
3.1. [← Назад], title, стек аватарок + «= N участников».
3.2. Артефакт-карточка вложения (paleo, synthesis, confidence-бейдж) +
     [Открыть в конструкторе ↗].
3.3. Комментарии: аватарка, handle, тело, опц. вложение-чип [↗],
     счётчики эмет/вопрос; реакции [эмет][вопрос] доступны researcher+.
3.4. Композер (researcher+): [📎 Прикрепить] + textarea + [Опубликовать];
     пикер вложения: стих/слово/доска/состояние/поиск + превью.
     Гостю вместо композера — «войдите, чтобы участвовать».

=== 4. ДАННЫЕ (адаптер js/club-data.js) ===
4.1. MOCK: 3–4 карточки (Шаббат·Быт2:2, Мицраим, состояние Шеол, доска)
     с participants (mock-профили: handle, уровень, цвет/инициал),
     posts_count, emet, questions; комментарии с вложениями.
4.2. Если Supabase-клиент есть — читать из research_threads/comments
     (схема как в прошлой постановке), иначе MOCK.
4.3. Публикация в MOCK — в память/localStorage (для теста), без сервера.

=== 5. КОМПОНЕНТ СТЕКА ===
Вынести renderAvatarStack(users, max=5) в общий хелпер — использовать
в карточках, детали и сайд-панели.

=== НЕ ЛОМАТЬ ===
- пункт «Палео-клуб» в сайдбаре с бейджем, роли, роутер, шапки/крошки,
  тренажёр, waitlist-адаптер. build/ не править вручную.

=== ПРОВЕРКА ===
- Гость: лента+деталь видны, композера нет, в панели waitlist.
- Dev-researcher: композер и реакции работают; пост добавляется (mock).
- Стек аватарок: 5 кружков + «+N»; клик → мини-профиль.
- [Открыть ↗] ведёт в нужный модуль на объекте; mobile — 1 колонка.

=== ОТЧЁТ ===
- изменённые файлы; структура MOCK; скрин ленты (desktop+mobile) и детали.