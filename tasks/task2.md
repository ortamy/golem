ЗАДАЧА: Палео-тренажёр v2 — только сборка, во всю ширину, компактнее

КОНТЕКСТ
- #learn/paleo-trainer, Vanilla JS, пергамент; есть режимы ОБРАЗ/СБОРКА,
  разбор с confidence, «Сгенерировать слово».
- build/ — производный; после правок собирать.

1. УБРАТЬ режим ОБРАЗ и переключатель. Остаётся ТОЛЬКО сборка слова.
2. КАРТОЧКА НА ВСЮ ШИРИНУ контейнера (убрать узкую центровку):
   - глифы: font-size clamp(80px,16vw,220px), ряд по центру, gap крупный;
   - под глифами стрелка-подсказка «‹‹‹ читается справа налево».
3. УБРАТЬ дубль вопроса «Что значит это слово?» (оставить один над полем).
4. РАЗБОР — горизонтальные колонки по буквам (не вертикальный стек):
   колонка = [глиф крупно, имя, образ, смысл]; на mobile 2 в ряд.
5. РАСКРЫТИЕ СЛОВА после «Показать разбор»: имя+транслит+глосса
   (напр. «ШАБАТ · шаббат · остановка») + авторская сборка крупно +
   бейдж confidence (сохранить).
6. КНОПКИ: [Показать разбор] [Сгенерировать слово] [Дальше →];
   счётчик «СЛОВО N/M» в шапке карточки; Enter в поле = показать разбор.
7. СРАВНЕНИЕ: после разбора показать «Твой ответ ↔ Сборка» с подсветкой
   совпавших ключевых образов (keywords из данных).
8. WEAVER-ПРАВИЛО: запретить глагол-когнат подлежащего
   («разрушение, которое разрушает» → «разрушение, которое останавливает»);
   при конфликте брать следующий глагол цепочки.
9. ПРОГРЕСС: слова N/M + буквы из разбора отмечаются в «Свиве» (как сейчас),
   мини-полоса 𐤀– в шапке карточки.
10. (ОПЦ) Тема «Ночь»: тёмный фон карточки, светлые глифы; тумблер.
11. Оберни в белый контейнер весь контент под шапкой

НЕ ЛОМАТЬ: генерацию из roots.json, confidence, маршруты, шапки/крошки.
build/ не править вручную.

ПРОВЕРКА
- Переключателя нет; вопрос один; карточка во всю ширину.
- Разбор — ряд колонок; слово раскрывается с именем/глоссой.
- «Дальше» листает слова; счётчик растёт; Enter работает.
- Нет «разрушение, которое разрушает».
- Mobile: колонки 2 в ряд, глифы вписываются.

ОТЧЁТ: изменённые файлы; скрин desktop+mobile; пример раскрытия слова.