// purgecss.config.js — PurgeCSS конфиг для удаления неиспользуемых CSS классов
// Запуск: npx purgecss --config purgecss.config.js

module.exports = {
  // Пути к HTML и JS файлам для сканирования
  content: [
    './*.html',
    './ru/**/*.html',
    './en/**/*.html',
    './he/**/*.html',
    './js/*.js',
    './app.js',
    './export/*.html',
    '../webapp/**/*.html',
  ],

  // Входной CSS файл
  css: ['./style.css'],

  // Выходной CSS файл (минифицированный)
  output: './style.min.css',

  // Настройки
  defaultExtractor: content => {
    return content.match(/[\w-/:]+(?<!:)/g) || [];
  },

  // Сохраняем все селекторы, используемые в проекте
  safelist: {
    standard: [
      /^cat-header/,
      /^file-item/,
      /^file-icon/,
      /^bookmark/,
      /^history/,
      /^toc-/,
      /^burger/,
      /^side-panel/,
      /^hamburger/,
      /^font-size/,
      /^sidebar/,
      /^top-bar/,
      /^footer/,
      /^hero/,
      /^section/,
      /^card/,
      /^thesis/,
      /^step/,
      /^btn/,
      /^lang/,
      /^research/,
      /^r-/,
      /^md-/,
      /^bc-/,
      /^spinner/,
      /^fade/,
      /^show$/,
      /^open$/,
      /^active$/,
      /^app-mode/,
      /^hebrew/,
      /^sr-only/,
      /^focus/,
      /^meander/,
      /^path-hint/,
      /^content-icon/,
      /^h2-icon/,
      /^icon$/,
      /^logo$/,
      /^loader$/,
      /^lang-grid$/,
      /^lang-link$/,
      /^footer-/,
      /^site-footer$/,
      /^footer-links$/,
      /^footer-copy$/,
      /^section-title$/,
      /^section-dark$/,
      /^section-inner$/,
      /^hero-content$/,
      /^hero-actions$/,
      /^card-grid$/,
      /^card-icon$/,
      /^thesis-icon$/,
      /^thesis-text$/,
      /^step-number$/,
      /^step-content$/,
      /^btn-primary$/,
      /^btn-secondary$/,
      /^side-panel-overlay$/,
      /^side-panel-header$/,
      /^side-panel-close$/,
      /^side-panel-links$/,
      /^side-panel-lang-switcher$/,
      /^logo-text$/,
      /^lang-switcher$/,
      /^research-header$/,
      /^research-filters$/,
      /^research-counter$/,
      /^research-list$/,
      /^research-item$/,
      /^r-title$/,
      /^r-cat$/,
      /^r-topic$/,
      /^file-view$/,
      /^research-view$/,
      /^back-to-list$/,
      /^page-view$/,
      /^md-content$/,
      /^file-page$/,
      /^file-top-bar$/,
      /^back-btn$/,
      /^file-path-hint$/,
      /^file-bookmark-btn$/,
      /^file-content-mobile$/,
      /^mobile-list-view$/,
      /^mobile-controls$/,
      /^stats-mobile$/,
      /^cat-header-mobile$/,
      /^file-item-mobile$/,
      /^burger-btn$/,
      /^burger-overlay$/,
      /^burger-menu$/,
      /^burger-logo$/,
      /^burger-actions$/,
      /^burger-item$/,
      /^bookmark-btn$/,
      /^bookmark-item$/,
      /^history-item$/,
      /^toc-panel$/,
      /^toc-header$/,
      /^toc-item$/,
      /^back-to-top$/,
      /^copy-toast$/,
      /^progress-bar$/,
      /^breadcrumbs$/,
      /^bc-sep$/,
      /^path-hint$/,
      /^empty$/,
      /^sr-only$/,
      /^hebrew$/,
      /^app-mode$/,
      /^fade-in$/,
    ],
  },
};